{
  "name": "bezier",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "antd": "^3.13.0",
    "classnames": "^2.2.6",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-scripts": "2.1.3",
    "styled-jsx": "^3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "eslint": "eslint src",
    "precommit-msg": "echo 'Pre-commit checks...' && exit 0"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "pre-commit": [
    "precommit-msg",
    "eslint"
  ],
  "homepage": "http://shlpro.site/bezier",
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "pre-commit": "^1.2.2",
    "react-app-rewired": "^2.0.3"
  }
}
